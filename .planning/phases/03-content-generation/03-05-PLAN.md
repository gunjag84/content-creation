---
phase: 03-content-generation
plan: "05"
type: tdd
wave: 1
depends_on: ["03-01"]
files_modified:
  - src/renderer/src/stores/useCreatePostStore.ts
  - src/renderer/src/pages/CreatePost.tsx
  - src/renderer/src/components/wizard/StepIndicator.tsx
  - src/renderer/src/components/wizard/Step1Recommendation.tsx
  - src/renderer/src/App.tsx
  - tests/renderer/stores/createPostStore.test.ts
autonomous: true
requirements:
  - POST-01
  - POST-02
  - POST-03
  - POST-04
  - POST-05
  - POST-07

must_haves:
  truths:
    - "Clicking 'Create' in sidebar shows the wizard full-page (replaces current content area)"
    - "Step 1 shows a recommendation card with pillar/theme/mechanic (or cold start empty state)"
    - "User can override each recommendation dimension via dropdown"
    - "User can toggle between AI and Manual mode via a labeled toggle on Step 1"
    - "User can optionally upload a custom background image on Step 1"
    - "User can type an impulse (free-text) on Step 1"
    - "Step indicator shows 5 steps with current step highlighted"
    - "Clicking 'Generate Content' (AI mode) advances to Step 2"
  artifacts:
    - path: "src/renderer/src/stores/useCreatePostStore.ts"
      provides: "Zustand wizard state: currentStep, selections, slides, caption, renderedPNGs, storyProposals, reset"
    - path: "src/renderer/src/pages/CreatePost.tsx"
      provides: "Wizard container that renders correct step component based on currentStep"
    - path: "src/renderer/src/components/wizard/StepIndicator.tsx"
      provides: "5-step progress indicator"
    - path: "src/renderer/src/components/wizard/Step1Recommendation.tsx"
      provides: "Step 1 UI: recommendation card, override dropdowns, mode toggle, impulse field, background upload"
  key_links:
    - from: "src/renderer/src/App.tsx"
      to: "src/renderer/src/pages/CreatePost.tsx"
      via: "'create' case in renderPage() switch"
      pattern: "case 'create'"
    - from: "src/renderer/src/components/wizard/Step1Recommendation.tsx"
      to: "src/renderer/src/stores/useCreatePostStore.ts"
      via: "useCreatePostStore() hooks for recommendations and selections"
      pattern: "useCreatePostStore"
---

<objective>
Build the Zustand wizard store, CreatePost page shell, and Step 1 (Recommendation & Selection) UI. This wires the 'Create' nav item to a functional first step.

Purpose: Step 1 is the entry point for every content creation session. Getting it right establishes the patterns (store shape, component structure) all subsequent wizard steps follow.
Output: Working wizard shell with navigable Step 1. Steps 2-5 render placeholders - they are implemented in Plans 06 and 07.
</objective>

<execution_context>
@C:/Users/tim/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/tim/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/03-content-generation/03-CONTEXT.md
@.planning/phases/03-content-generation/03-RESEARCH.md

<interfaces>
<!-- Existing patterns and types to build against -->

From src/renderer/src/App.tsx (existing):
```typescript
type NavItem = 'dashboard' | 'create' | 'settings'
// renderPage() switch has 'create' case returning <TestRender /> - replace with <CreatePost />
```

From src/renderer/src/stores/settingsStore.ts (pattern reference):
```typescript
import { create } from 'zustand'
// Zustand store pattern established in Phase 2
```

From src/shared/types/generation.ts (Plan 01):
```typescript
export interface Slide {
  id?: number
  slide_number: number
  slide_type: 'cover' | 'content' | 'cta'
  hook_text: string
  body_text: string
  cta_text: string
  overlay_opacity: number
  custom_background_path?: string
}

export interface BalanceRecommendation {
  pillar: string
  theme: string
  mechanic: string
  reasoning: 'cold_start_round_robin' | 'performance_weighted'
}

export interface BalanceWarning {
  variable_type: 'pillar' | 'mechanic' | 'theme'
  variable_value: string
  usage_count: number
  days_span: number
  message: string
}

export interface StoryProposal { ... }
export interface ExportFile { ... }
```

shadcn/ui components already installed:
- Button, Card, Input, Label, Select, Separator, Slider, Switch, Tabs, Textarea
- Import from '@/components/ui/*' (all in src/renderer/src/components/ui/)

Tailwind dark theme pattern from existing components:
- Background: bg-slate-900 / bg-slate-800
- Text: text-slate-100 / text-slate-400
- Border: border-slate-700
</interfaces>
</context>

<feature>
  <name>Wizard Store + CreatePost Shell + Step 1</name>
  <files>
    src/renderer/src/stores/useCreatePostStore.ts,
    tests/renderer/stores/createPostStore.test.ts
  </files>
  <behavior>
    Zustand store shape:
    - currentStep: 1 | 2 | 3 | 4 | 5 (default 1)
    - mode: 'ai' | 'manual' (default 'ai')

    Step 1 state:
    - recommendation: BalanceRecommendation | null
    - warnings: BalanceWarning[]
    - selectedPillar: string
    - selectedTheme: string
    - selectedMechanic: string
    - contentType: 'single' | 'carousel' (default 'carousel')
    - impulse: string
    - customBackgroundPath: string | null

    Step 2/3 state:
    - generatedSlides: Slide[]
    - caption: string
    - isGenerating: boolean
    - generationError: string | null

    Step 4 state:
    - renderedPNGs: string[]  // base64 data URLs
    - postId: number | null   // DB id after save

    Step 5 state:
    - storyProposals: StoryProposal[]
    - approvedStories: Set<number>  // indices
    - exportFolder: string | null

    Actions:
    - setStep(step): void
    - setMode(mode): void
    - setRecommendation(rec, warnings): void
    - setSelection(field, value): void
    - setSlide(index, field, value): void
    - reorderSlides(fromIndex, toIndex): void
    - setCaption(text): void
    - appendGeneratingToken(token): void
    - setGenerationComplete(result: GenerationResult): void
    - setRenderedPNGs(pngs): void
    - setPostId(id): void
    - setStoryProposals(proposals): void
    - toggleStoryApproval(index): void
    - setExportFolder(path): void
    - reset(): void

    Tests (turning RED stubs GREEN):
    - setSlide: index 1, field 'hook_text', value 'New hook' -> slides[1].hook_text === 'New hook'
    - reorderSlides: [A, B, C] reorder(0, 2) -> [B, C, A]
    - reset: after setting multiple fields, reset() -> all back to initial values
    - setStep: setStep(3) -> currentStep === 3
  </behavior>
  <implementation>
    TDD cycle:

    1. Replace createPostStore.test.ts stubs with real tests using actual store

    2. Implement useCreatePostStore.ts with create<CreatePostState>()(set => ({ ... }))
       - Initial state covers all fields above
       - reorderSlides uses splice pattern: const items = [...slides]; const [moved] = items.splice(from, 1); items.splice(to, 0, moved)
       - appendGeneratingToken: accumulates tokens into generatedSlides[activeSlide] or a streaming buffer field
       - setGenerationComplete: parses GenerationResult into Slide array

    3. Create Step 1 components (non-TDD, UI code):

    **StepIndicator.tsx:**
    - 5 numbered circles in a row connected by lines
    - Current step: filled primary color circle
    - Completed steps: checkmark icon
    - Pending steps: outlined circle with number
    - Props: currentStep: 1|2|3|4|5, labels: string[]

    **Step1Recommendation.tsx:**
    - Load recommendation on mount via window.api.posts.getRecommendationData()
    - Show recommendation card: "Based on rotation balance:" with pillar/theme/mechanic
    - If cold start (no recommendation): show "Starting fresh - equal rotation active" card
    - Override section: three Select dropdowns (pillar, theme, mechanic) pre-populated from recommendation
    - Show BalanceWarning badge/tooltip on affected dropdown (amber dot + hover tooltip with message)
    - "Content Type" row: two radio-style buttons "Single Post" / "Carousel"
    - "Impulse" textarea: optional, placeholder "Guide AI with a specific angle or topic..."
    - "Custom Background" button: calls window.api.templates.uploadBackground(), stores path
    - Mode toggle: labeled Switch "AI Generation" / "Manual" - toggles mode in store
    - Primary CTA button:
      - AI mode: "Generate Content" (advances to Step 2)
      - Manual mode: "Fill In Manually" (advances directly to Step 3 with empty slides)
    - Settings for dropdowns: load settings via window.api.loadSettings() to populate pillar/theme/mechanic options

    **CreatePost.tsx:**
    - Import useCreatePostStore
    - Render StepIndicator at top with labels ['Recommendation', 'Generate', 'Edit', 'Render', 'Stories']
    - Switch on currentStep: render Step1Recommendation (step 1), placeholder divs for steps 2-5
    - "Exit" button top-right: calls reset() then calls onNavigate('dashboard') via prop

    4. Update App.tsx:
       - Import CreatePost
       - Replace <TestRender /> with <CreatePost onNavigate={handleNavigate} /> in 'create' case
  </implementation>
</feature>

<verification>
  <automated>cd "C:/webprojects/content-creation" && npm test tests/renderer/stores/createPostStore.test.ts 2>&1 | tail -20</automated>
</verification>

<success_criteria>
createPostStore.test.ts passes GREEN (all 4 tests). Wizard renders when clicking Create in sidebar. Step 1 shows recommendation card or cold start state. Override dropdowns populated. Mode toggle visible. Step indicator shows 5 steps. npm test suite passes.
</success_criteria>

<output>
After completion, create `.planning/phases/03-content-generation/03-05-SUMMARY.md`
</output>
