---
phase: 03.1-visual-slide-editor
plan: 04
type: execute
wave: 3
depends_on: [03.1-02, 03.1-03]
files_modified:
  - src/renderer/src/components/wizard/SlidePresetManager.tsx
  - src/renderer/src/components/wizard/Step3EditText.tsx
autonomous: false
requirements: [VSED-06, VSED-08]

must_haves:
  truths:
    - "User can save current zone overrides as a named preset"
    - "User can select a saved preset from a dropdown and apply it to the active slide"
    - "User can delete a saved preset"
    - "Applied preset changes are visible immediately in LivePreview"
  artifacts:
    - path: "src/renderer/src/components/wizard/SlidePresetManager.tsx"
      provides: "Preset save/load/delete UI"
      exports: ["SlidePresetManager"]
    - path: "src/renderer/src/components/wizard/Step3EditText.tsx"
      provides: "Step 3 with integrated preset manager"
      contains: "SlidePresetManager"
  key_links:
    - from: "src/renderer/src/components/wizard/SlidePresetManager.tsx"
      to: "src/preload/types.ts"
      via: "window.api.presets.list/save/delete"
      pattern: "window.api.presets"
    - from: "src/renderer/src/components/wizard/SlidePresetManager.tsx"
      to: "src/renderer/src/stores/useCreatePostStore.ts"
      via: "applyPreset action"
      pattern: "applyPreset"
---

<objective>
Build the SlidePresetManager UI component for saving, loading, and deleting zone override presets, integrate it into Step 3, and verify the full visual editor experience with a human checkpoint.

Purpose: Presets complete the visual editor by letting users save and reuse zone override configurations across slides and sessions.
Output: Working preset UI in Step 3, full visual editor verified by user.
</objective>

<execution_context>
@C:/Users/tim/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/tim/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/03.1-visual-slide-editor-per-slide-zone-positioning-font-controls-undo-redo-presets/03.1-RESEARCH.md
@.planning/phases/03.1-visual-slide-editor-per-slide-zone-positioning-font-controls-undo-redo-presets/03.1-01-SUMMARY.md
@.planning/phases/03.1-visual-slide-editor-per-slide-zone-positioning-font-controls-undo-redo-presets/03.1-02-SUMMARY.md
@.planning/phases/03.1-visual-slide-editor-per-slide-zone-positioning-font-controls-undo-redo-presets/03.1-03-SUMMARY.md

<interfaces>
From src/shared/types/generation.ts (after Plans 01+02):
```typescript
export interface SlidePreset {
  id: string
  name: string
  template_id?: number
  zone_overrides: Record<string, ZoneOverride>
  overlay_opacity?: number
  created_at: number
}
```

From src/preload/types.ts (after Plan 02):
```typescript
presets: {
  list: () => Promise<SlidePreset[]>
  save: (preset: SlidePreset) => Promise<void>
  delete: (id: string) => Promise<void>
}
```

From src/renderer/src/stores/useCreatePostStore.ts (after Plans 01+02):
```typescript
applyPreset: (slideIndex: number, preset: SlidePreset) => void
setZoneOverride: (slideIndex: number, zoneId: string, override: Partial<ZoneOverride>) => void
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: SlidePresetManager component + integration into Step 3</name>
  <files>
    src/renderer/src/components/wizard/SlidePresetManager.tsx,
    src/renderer/src/components/wizard/Step3EditText.tsx
  </files>
  <read_first>
    src/renderer/src/components/wizard/Step3EditText.tsx,
    src/renderer/src/components/wizard/SlideZoneOverrides.tsx,
    src/renderer/src/stores/useCreatePostStore.ts,
    src/shared/types/generation.ts,
    src/preload/types.ts
  </read_first>
  <action>
    1. Create src/renderer/src/components/wizard/SlidePresetManager.tsx:
       - Props: `{ slideIndex: number, templateId?: number }`
       - Local state:
         - `presets: SlidePreset[]` - loaded from IPC
         - `saveName: string` - input for new preset name
         - `showSaveInput: boolean` - toggle save name field
         - `isLoading: boolean`
       - On mount useEffect: call `window.api.presets.list()` and set to local state
       - Filter presets: show all presets (future: filter by template_id)

       - UI Layout (compact horizontal strip, below SlideZoneOverrides):
         ```
         [ Apply Preset: [dropdown] ]  [ Save as Preset ]
         ```
         When "Save as Preset" clicked, show inline input:
         ```
         [ Name: [___________] ] [ Save ] [ Cancel ]
         ```

       - Apply preset dropdown:
         - Use a native `<select>` with dark theme styling (bg-slate-800, border-slate-600, text-slate-200)
         - First option: "Select preset..." (disabled, selected by default)
         - Each preset option shows: `preset.name`
         - onChange: get the selected preset, call `useCreatePostStore.getState().applyPreset(slideIndex, preset)`

       - Save as Preset button:
         - Toggles `showSaveInput` to true
         - Input field: `<input>` with bg-slate-800, border-slate-600, text-slate-200, placeholder="Preset name"
         - Save button: Creates SlidePreset object:
           ```typescript
           const slide = useCreatePostStore.getState().generatedSlides[slideIndex]
           const preset: SlidePreset = {
             id: crypto.randomUUID(),
             name: saveName,
             template_id: templateId,
             zone_overrides: slide.zone_overrides ?? {},
             overlay_opacity: slide.overlay_opacity,
             created_at: Date.now()
           }
           await window.api.presets.save(preset)
           ```
         - After save: reload presets list, clear saveName, hide input

       - Delete preset: Small trash icon button (Trash2 from lucide-react, 14px) next to each preset in the dropdown
         - Alternatively: when a preset is selected, show a small "Delete" button
         - onClick: `await window.api.presets.delete(presetId)`, reload list

       - Dark theme consistent with app: bg-slate-800 for containers, slate-300 for text, slate-600 for borders
       - Import Button from '../ui/button', Save and Trash2 from lucide-react

    2. Update Step3EditText.tsx:
       - Import SlidePresetManager
       - Add below the SlideZoneOverrides section:
         ```tsx
         {zones.length > 0 && (
           <SlidePresetManager
             slideIndex={activeSlideIndex}
             templateId={template?.id}
           />
         )}
         ```
       - No other changes needed in Step3 - all preset logic is self-contained in SlidePresetManager
  </action>
  <verify>
    <automated>npm run build 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - src/renderer/src/components/wizard/SlidePresetManager.tsx exists and exports `SlidePresetManager`
    - SlidePresetManager.tsx contains `window.api.presets.list()` (loads presets on mount)
    - SlidePresetManager.tsx contains `window.api.presets.save(` (saves new preset)
    - SlidePresetManager.tsx contains `window.api.presets.delete(` (deletes preset)
    - SlidePresetManager.tsx contains `applyPreset` (applies preset to slide)
    - SlidePresetManager.tsx contains `crypto.randomUUID()` (generates preset ID)
    - Step3EditText.tsx contains `import { SlidePresetManager }`
    - Step3EditText.tsx contains `<SlidePresetManager` JSX
    - `npm run build` completes without TypeScript errors
  </acceptance_criteria>
  <done>SlidePresetManager saves/loads/deletes/applies presets via IPC, integrated into Step 3 below zone overrides</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Verify full visual slide editor experience</name>
  <files>none</files>
  <read_first>
    src/renderer/src/components/wizard/Step3EditText.tsx,
    src/renderer/src/components/wizard/SlideZoneOverrides.tsx,
    src/renderer/src/components/wizard/SlidePresetManager.tsx,
    src/renderer/src/components/wizard/LivePreview.tsx
  </read_first>
  <action>
    Human verifies the complete visual slide editor built across all 4 plans.
    No code changes - this is a visual/functional verification checkpoint.
    Start app with `npm run dev` and follow the verification steps below.
  </action>
  <what-built>
    Complete visual slide editor in Step 3:
    - Per-slide zone override controls (font size, weight, color, X/Y offset)
    - Live preview showing actual template render with overrides
    - Undo/redo (buttons + Ctrl+Z/Ctrl+Y keyboard shortcuts)
    - Preset save/load/delete
  </what-built>
  <how-to-verify>
    1. Start the app: `npm run dev`
    2. Navigate to Create Post wizard
    3. Complete Step 1 (select pillar, theme, mechanic) and generate content (Step 2)
    4. In Step 3:
       a. Verify "Zone Overrides" section appears below the overlay opacity slider
       b. Verify zone controls show for each zone (hook, body, cta) with font size slider, weight toggle, color swatch, X/Y offset
       c. Change the hook zone font size slider - verify the live preview (right panel) updates
       d. Change body zone color to red (#ff0000) - verify preview shows red body text
       e. Adjust X offset slider for a zone - verify the zone moves in preview
       f. Click Undo button (or Ctrl+Z) - verify the last change reverts
       g. Click Redo button (or Ctrl+Y) - verify the change re-applies
       h. Click "Save as Preset" - enter a name - click Save - verify it appears in the dropdown
       i. Change some overrides, then select the saved preset from dropdown - verify overrides revert to preset values
       j. Delete the preset - verify it disappears from dropdown
    5. Proceed to Step 4 - verify rendered PNGs reflect the zone overrides from Step 3
  </how-to-verify>
  <verify>
    <automated>npm test</automated>
  </verify>
  <done>User approves the full visual slide editor workflow: zone overrides, live preview, undo/redo, and presets all working correctly</done>
  <resume-signal>Type "approved" if all checks pass, or describe any issues found</resume-signal>
</task>

</tasks>

<verification>
- `npm run build` compiles without errors
- `npm test` passes (full suite)
- Visual verification of complete editor workflow
</verification>

<success_criteria>
- Preset manager UI works: save, load, apply, delete
- Full editor workflow verified by human: overrides -> preview -> undo/redo -> presets -> render
- No regressions in existing Step 3 or Step 4 functionality
</success_criteria>

<output>
After completion, create `.planning/phases/03.1-visual-slide-editor-per-slide-zone-positioning-font-controls-undo-redo-presets/03.1-04-SUMMARY.md`
</output>
