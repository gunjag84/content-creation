---
phase: 02-settings-templates
plan: 11
type: execute
wave: 1
depends_on: []
files_modified:
  - src/renderer/src/components/settings/BrandGuidanceSection.tsx
  - src/renderer/src/components/settings/FontUpload.tsx
autonomous: true
gap_closure: true
requirements: [SET-07]

must_haves:
  truths:
    - "Brand Guidance left column uses dark theme - text is readable against dark background"
    - "Standard fonts dropdown appears for all three font slots (Headline, Body, CTA) regardless of configured state"
    - "Large 'quick brown fox' font preview block is removed from FontUpload configured state"
    - "All gray-* color classes replaced with slate-* equivalents in both files"
  artifacts:
    - path: "src/renderer/src/components/settings/BrandGuidanceSection.tsx"
      provides: "Dark-themed left column and right preview column"
      contains: "bg-slate-800 border-slate-700"
    - path: "src/renderer/src/components/settings/FontUpload.tsx"
      provides: "Standard fonts dropdown always visible, no large preview block"
      contains: "STANDARD_FONTS"
  key_links:
    - from: "BrandGuidanceSection.tsx"
      to: "FontUpload.tsx"
      via: "props: fontConfig, onUpload, onRemove"
      pattern: "FontUpload"
---

<objective>
Fix dark theme on Brand Guidance left column and right preview wrapper. Move standard fonts dropdown from "empty state only" to always visible in FontUpload. Remove the large font preview block.

Purpose: Brand colors card is currently white-on-white (unreadable). Standard font selector is hidden for Headline/Body slots once a font is configured. Oversized font preview takes up space while the right-side preview panel already provides visual feedback.
Output: Dark-themed Brand Guidance section with consistent font selection UX across all three font slots.
</objective>

<execution_context>
@C:/Users/tim/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/tim/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Dark theme BrandGuidanceSection.tsx</name>
  <files>src/renderer/src/components/settings/BrandGuidanceSection.tsx</files>
  <action>
    Apply dark theme to both columns and all internal text elements:

    Line 60 - Left column wrapper: change `bg-white rounded-lg p-6 border border-gray-200` to `bg-slate-800 rounded-lg p-6 border border-slate-700`

    Line 179 - Right preview column wrapper: change `bg-white rounded-lg p-6 border border-gray-200 sticky top-6` to `bg-slate-800 rounded-lg p-6 border border-slate-700 sticky top-6`

    All heading elements using `text-gray-900` (lines 63, 88, 167) - change to `text-slate-100`

    All separator dividers using `border-gray-200` (lines 84, 163) - change to `border-slate-700`

    All label elements using `text-gray-700` (lines 115, 126, 137, 148) - change to `text-slate-300`

    All number inputs using `border-gray-300`, `focus:ring-blue-500` classes - change border to `border-slate-600`, background to `bg-slate-700`, text to `text-slate-100`, focus ring to `focus:ring-blue-500` (keep), add `focus:border-transparent`

    Full input class replacement pattern: `px-3 py-2 text-sm border border-gray-300 rounded-md focus:outline-none focus:ring-2 focus:ring-blue-500` becomes `px-3 py-2 text-sm bg-slate-700 border border-slate-600 text-slate-100 rounded-md focus:outline-none focus:ring-2 focus:ring-blue-500`
  </action>
  <verify>npm run typecheck 2>&1 | tail -5</verify>
  <done>Both column wrappers use bg-slate-800 border-slate-700. All text is readable (slate-100/slate-300). Inputs styled for dark context.</done>
</task>

<task type="auto">
  <name>Task 2: FontUpload always-visible dropdown and remove large preview</name>
  <files>src/renderer/src/components/settings/FontUpload.tsx</files>
  <action>
    Three changes to FontUpload.tsx:

    **1. Remove large font preview block (lines 125-135):**
    Delete the entire `div` block that renders "The quick brown fox jumps over the lazy dog" with dynamic fontFamily/fontSize style. This is the block inside the `{fontConfig ? (...) : (...)}` configured state branch.

    **2. Add standard fonts dropdown to configured state:**
    In the `fontConfig ? (...)` branch (configured state), add the standard fonts dropdown BEFORE the "Font info and actions" div. The dropdown should call `handleStandardFontSelect` when changed. Use the same dropdown JSX as the else-branch but without the wrapping `flex flex-col gap-2` label. Add a note "Switch font:" as a small label above it.

    The configured state branch should then contain:
    - Standard font selector (new)
    - A divider "or"
    - Font info row (filename + Change + Remove buttons)

    **3. Fix all color classes to dark theme:**
    - `text-gray-700` label (line 100) -> `text-slate-300`
    - `text-gray-600` (filename display, line 106) -> `text-slate-400`
    - `text-xs text-gray-600` (dropdown sub-label) -> `text-xs text-slate-400`
    - Select dropdown: `border-gray-300` -> `border-slate-600`, add `bg-slate-700 text-slate-100`
    - Divider: `border-gray-300` -> `border-slate-600`, `text-gray-500` -> `text-slate-500`
    - Upload button: keep `text-white bg-blue-600 hover:bg-blue-700` (already correct)
    - `text-xs text-gray-500` (file format note) -> `text-xs text-slate-500`
    - `Change` and `Remove` button link colors: `text-blue-600 hover:bg-blue-50` -> `text-blue-400 hover:bg-blue-900/20`, `text-red-600 hover:bg-red-50` -> `text-red-400 hover:bg-red-900/20`

    After edit, selectedStandardFont state should initialize from fontConfig.family when fontConfig is a standard font (path === ''). Add a useEffect:
    ```
    useEffect(() => {
      if (fontConfig && !fontConfig.path) {
        setSelectedStandardFont(fontConfig.family)
      } else {
        setSelectedStandardFont('')
      }
    }, [fontConfig?.family, fontConfig?.path])
    ```
    This keeps the dropdown in sync when switching between slots.
  </action>
  <verify>npm run typecheck 2>&1 | tail -5</verify>
  <done>Standard fonts dropdown visible in both configured and unconfigured states. Large 'quick brown fox' block removed. All text uses slate-* colors. TypeScript passes.</done>
</task>

</tasks>

<verification>
After both tasks: start the dev app and navigate to Settings > Brand Guidance.
- Left column card has dark background (readable text)
- Right preview column wrapper has dark background
- All three FontUpload slots show the standard fonts dropdown (regardless of whether a font is configured)
- No "quick brown fox" preview visible in any slot
- Color inputs are readable in dark context
</verification>

<success_criteria>
Brand Guidance section is fully dark-themed. All font slots show the standard fonts dropdown. The oversized font preview is gone. No white cards, no unreadable text.
</success_criteria>

<output>
After completion, create `.planning/phases/02-settings-templates/02-11-SUMMARY.md`
</output>
